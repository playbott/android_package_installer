name: android_package_installer_example
description: "Demonstrates how to use the android_package_installer plugin."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^12.0.0+1
  filesystem_picker: ^4.1.0
  android_package_installer:
    path: ../


flutter:
  uses-material-design: true
